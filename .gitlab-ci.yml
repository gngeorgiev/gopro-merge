image: rust:latest
variables:
  RUST_LOG: debug

check:
  stage: test
  before_script:
    - apt -y update
    - apt install -y ffmpeg
    - rustup component add clippy rustfmt
  script:
    - cargo test
    - cargo clippy -- -D warnings
    - cargo fmt -- --check
